name: Playwright E2E Tests

on:
  # Auto-run on push/PR is on hold until further notice — remove the comment
  # markers below to re-enable. In the meantime, use workflow_dispatch (the
  # Actions tab "Run workflow" button) to trigger manually.
  # push:
  #   branches: [main]
  # pull_request:
  #   branches: [main]
  # Daily scheduled run disabled — was firing automatically and consuming CI
  # minutes/quota unnecessarily. Re-add if a nightly run is wanted again:
  # schedule:
  #   - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      browser:
        description: 'Browser'
        required: false
        default: 'chromium'
        type: choice
        options: [chromium, firefox, webkit]
      grep:
        description: 'Test filter (grep)'
        required: false
        default: ''

# Non-sensitive config hardcoded — no secrets warnings
env:
  BASE_URL:   'https://dev.liveaccess.ai'
  LOGIN_URL:  'https://dev.liveaccess.ai/login'
  HEADLESS:   'true'
  WORKERS:    '4'
  RETRIES:    '2'

jobs:
  test:
    name: E2E Tests – ${{ matrix.browser }}
    runs-on: ubuntu-latest
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps ${{ matrix.browser }}

      - name: Run tests
        run: npx playwright test --project=${{ matrix.browser }}
        env:
          CI:            'true'
          USER_EMAIL:    ${{ secrets.USER_EMAIL }}
          USER_PASSWORD: ${{ secrets.USER_PASSWORD }}

      - name: Upload HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-${{ matrix.browser }}
          path: playwright-report/
          retention-days: 14

      - name: Upload Allure results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results-${{ matrix.browser }}
          path: allure-results/
          retention-days: 14

      - name: Upload failure artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-artifacts-${{ matrix.browser }}
          path: test-results/
          retention-days: 7
